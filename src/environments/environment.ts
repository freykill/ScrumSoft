// Ambiente por defecto (local). Se reemplaza en build segun la configuracion
// usada, ver `fileReplacements` en angular.json.

export const environment = {
    production: false,
    name: 'local',
    urlBackend: 'https://localhost:7086'
};
